cmake_minimum_required(VERSION 3.16)

# add_subdirectory(../../lib lib)

# include_directories(${SHARED_LIB_INCLUDE_DIR})

# set(CMAKE_CXX_FLAGS "-std=c++2a -lpthread")

# set(SRCS ml.cpp)
# add_executable(arith ${SRCS})
# target_link_libraries(ml lib)